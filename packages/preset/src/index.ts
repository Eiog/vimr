import type { Preset } from 'unocss'

export function presetVimr(): Preset {
  return {
    name: '@vimr-component/preset',
    shortcuts: {
      // 'vimr-wrap': 'w-full flex flex-col',
      // 'vimr-header': 'w-full flex items-center ',
    },
  }
}
